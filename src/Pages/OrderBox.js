import React from "react";
import Order from "./Order";
const OrderBox = ({ dbUsers, handleOnDelete }) => {
  // const { name, prices, quantity, img, _id } = dbUsers;
  return (
    <div>
      <Order></Order>
    </div>
  );
};

export default OrderBox;
